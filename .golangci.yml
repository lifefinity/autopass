version: "2"

run:
  timeout: 3m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosec
    - revive
    - misspell

  settings:
    gosec:
      excludes:
        - G101
        - G204
        - G304
    revive:
      rules:
        - name: exported
          disabled: true

formatters:
  enable:
    - gofmt

issues:
  exclude-dirs:
    - testdata
  max-issues-per-linter: 0
  max-same-issues: 0
